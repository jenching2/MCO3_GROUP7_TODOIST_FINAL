<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobdeve.s13.ching.jennilyn.mco3mobdeve">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCO3MOBDEVE">
        <activity android:name=".SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".CalendarActivity" /> <!-- Ensure CalendarActivity is declared -->
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".DueTodayActivity" />
        <activity android:name=".UpcomingTasksActivity" />
        <activity android:name=".OverdueTasksActivity" />

    </application>
</manifest>
